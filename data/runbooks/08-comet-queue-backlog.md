# Comet 消息积压初步排查

适用系统：Comet 订单事件消费者  
环境：测试与预发布  
负责人：韩川

首先在 MetricsHub 查看 `comet_consumer_lag`。积压小于 10,000 条且持续下降时继续观察；持续 10 分钟上升时，检查消费者实例是否 Ready，并查看最近一次发布记录。

若存在反复重试的消息，记录 topic、partition、offset 和错误码，不要直接删除消息。测试环境可将消费者扩容到最多 4 个实例。预发布环境扩容前必须通知值班负责人。

本文只描述初步排查，不包含生产故障处置和死信重放。