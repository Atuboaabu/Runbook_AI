# Zenith 定时任务临时停用记录

2026-02-11 因测试数据异常，曾在调度平台暂停 `zenith-daily-summary` 任务。当时的操作是进入 SchedulerX，选择 test 环境并点击暂停。

这是一份事件记录，不是正式操作手顺。文档没有描述重新启用、验证、审批或回退步骤。后续如需暂停 Zenith 定时任务，应先向值班负责人确认，不得仅依据本文执行。

记录人：叶舟；状态：待复核。