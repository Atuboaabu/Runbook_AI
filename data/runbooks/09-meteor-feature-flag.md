# Meteor 测试环境功能开关配置

进入 FeatureHub，选择项目 Meteor 和环境 test。创建开关时使用 `team_feature_purpose` 命名格式，默认值必须为关闭。添加研发测试账号白名单后提交审核。

审核通过后先将流量比例设为 5%，观察 15 分钟错误率；无异常再调整。关闭开关时保留配置记录，不要删除开关。

验证方法：在 FeatureHub 的“评估记录”中搜索测试账号，确认命中规则和返回值。负责人：产品研发联合小组；更新日期：2026-03-21。