# Sentinel 测试证书更新（新版）

状态：已发布  
版本：v2  
更新时间：2026-07-30  
适用环境：test

在证书管理台创建更新申请，目标选择 Sentinel-test。上传公钥证书链，不得上传私钥文件。审批通过后点击“发布”，系统会自动滚动更新网关实例。

发布后执行 `sentinelctl cert status --env test`，确认到期时间和指纹与申请单一致。失败时不要登录服务器手工替换，应在管理台执行“回退上一版本”。

负责人：平台组。