# Pulsar 缓存清理手顺

系统：Pulsar 配置中心；更新日期：2026-04-09

遇到配置发布后测试环境仍读取旧值时，可在内部运维台进入“Pulsar → Cache → Namespace”，输入命名空间并选择“预览影响”。确认键数量不超过 200 后，点击“清理测试缓存”。

清理后等待 30 秒，再使用配置查询页面确认版本号。若键数量超过 200，停止操作并联系平台组。生产缓存清理不在本手顺范围内。

注意：不要使用 Redis 客户端直接执行 FLUSHDB。